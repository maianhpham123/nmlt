#include <bits/stdc++.h>

int main() {
    int n;
    std::cin >> n;

    int N = 2 * n + 1;
    int result = 0;

    for (int i = 0; i < N; i++) {
        int number;
        std::cin >> number;
        result ^= number; // XOR từng số với biến result
    }

    std::cout << result << std::endl;

    return 0;
}
